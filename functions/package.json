{
  "name": "comfort-business-hub-functions",
  "scripts": {
    "lint": "tslint \"src/**/*.ts\"",
    "build": "tsc",
    "serve": "npm run build && firebase serve --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "18"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@react-email/components": "^0.0.31",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "react": "^18.2.0",
    "resend": "^3.2.0"
  },
  "devDependencies": {
    "@types/node": "^18.15.0",
    "@types/react": "^18.2.0",
    "typescript": "^5.0.0"
  },
  "private": true
}
